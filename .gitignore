# virtual environment
.venv/
venv/
.env

# Raw data
data/raw/
*.csv
# Allow processed data
!data/processed/**

# Build artifacts, rebuilt from the notebooks (do not commit)
*.duckdb
data/processed/cache/
*.parquet
# stray Colab downloads landing next to the notebooks (the data zip lives in data/processed/)
notebooks/*.zip

# Draft code/notebooks
drafts